# Recipes to geocode data with Google API.

**Requests** is the only requirement for it. Do not forget to replace `YOUR_API_KEY` with your Google API key to use the recipes.

Run `geocoding.py` to see some examples.
